import { TransactionTypeValidatorDirective } from './transaction-type-validator.directive';

describe('TransactionTypeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TransactionTypeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
